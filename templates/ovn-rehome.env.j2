# Managed by OpenStack re-home playbook.
# Example only. Your distro may store these values in /etc/openvswitch/conf.db or systemd drop-ins.
OVN_REMOTE={{ target_ovn_remote | default('tcp:TARGET_SB_DB_IP:6642') }}
OVN_ENCAP_IP={{ target_ovn_encap_ip | default(ansible_default_ipv4.address) }}
OVN_ENCAP_TYPE={{ target_ovn_encap_type | default('geneve') }}
OVN_CHASSIS_NAME={{ rehome_host }}
